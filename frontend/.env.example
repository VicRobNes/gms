# Only needed if you deploy the API as a separate Vercel project (different
# origin from the UI). When the API is mounted as a Next.js route in this app
# (the default), leave this unset — the client uses same-origin fetches.
# NEXT_PUBLIC_API_BASE_URL=https://your-backend.vercel.app
