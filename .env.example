# Backend (only used when wiring Prisma; the in-memory store works without this)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tourism_crm

# Optional: lock down CORS in production by setting a comma-separated allowlist.
# Leave unset to mirror the request origin (suitable for local dev).
# CORS_ORIGINS=https://crm.example.com,https://staging.crm.example.com
